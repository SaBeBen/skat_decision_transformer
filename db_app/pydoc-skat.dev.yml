version: '3.9'

services:
 web:
  build:
   context: .
  ports:
  - 8000:5000
  volumes:
  - ./:/app

 mysqldb:
  image: mysql
  ports:
  - 3306:3306
  environment:
   MYSQL_HOST: mysqldb
   MYSQL_PASSWORD: Pgooelx15
   MYSQL_ROOT_PASSWORD: Pgooelx15!
  volumes:
  - mysql_skat:/var/lib/mysql
  - mysql_skat_config:/etc/mysql

volumes:
  mysql_skat:
  mysql_skat_config:
